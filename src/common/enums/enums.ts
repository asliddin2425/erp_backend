export enum SubmissionStatus {
  PENDING = "pending",
  MARKING = "marking",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
}

export enum Role {
  STUDENT = "student",
  TEACHER = "teacher",
  ADMIN = "admin",
  SUPERADMIN = "superadmin",
}

export enum GroupStatus {
  PENDING = "pending",
  ACTIVE = "active",
  FINISHED = "finished",
  CANCELED = "canceled",
}
